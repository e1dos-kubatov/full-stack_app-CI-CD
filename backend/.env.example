PORT=5000
DB_CONNECT_RETRIES=10
DB_CONNECT_DELAY=1

# Use one of these options:
# 1. PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost:5432/fullstack_app
#
# 2. SQLite (easy deploy / local fallback)
# SQLITE_PATH=./instance/app.db
